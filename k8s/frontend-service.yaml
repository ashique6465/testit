apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: api-testing
  labels:
    app: api-testing-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: api-testing-frontend
