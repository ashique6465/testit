# Template - fill values locally with kubectl create secret or via CI
# Example: kubectl create secret generic backend-secrets --namespace api-testing \
#   --from-literal=MONGO_URI='mongodb+srv://user:pass@cluster/mydb' \
#   --from-literal=DB_NAME='testdb' \
#   --from-literal=JWT_SECRET='change-this'

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: api-testing
type: Opaque
stringData:
  MONGO_URI: "REPLACE_WITH_MONGO_URI"
  DB_NAME: "REPLACE_WITH_DB_NAME"
  JWT_SECRET: "REPLACE_WITH_JWT_SECRET"
