apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: api-testing
  labels:
    app: api-testing-backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: api-testing-backend
